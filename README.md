# Netflix Catalog 🎬

Веб-приложение для работы с каталогом Netflix. Позволяет искать и фильтровать фильмы и сериалы по различным параметрам.

## 🚀 Быстрый старт

Для запуска приложения достаточно одной команды:

```bash
docker compose up --build
```

После запуска приложение будет доступно по адресу: http://localhost:8000

## 📋 Функционал

- **Авторизация**: Регистрация и вход пользователей с JWT токенами
- **Поиск**: Полнотекстовый поиск по названию, режиссёру, актёрам и описанию
- **Фильтры**:
  - По типу контента (Movie / TV Show)
  - По категориям (Action, Comedy, Drama и др.)
  - По рейтингу (TV-MA, TV-14, PG-13 и др.)
  - По году выпуска (от и до)
- **Пагинация**: Удобная навигация по страницам

## 🔐 Демо-аккаунт

После первого запуска автоматически создаётся демо-пользователь:

- **Username**: `demo`
- **Password**: `demo123`

## 🛠 Технологии

- **Backend**: FastAPI, SQLAlchemy, Pydantic
- **Database**: PostgreSQL 15
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **Data Processing**: Pandas
- **Auth**: JWT (python-jose), Passlib (bcrypt)
- **Containerization**: Docker, Docker Compose

## 📁 Структура проекта

```
netflix-catalog/
├── app/
│   ├── api/               # API endpoints
│   │   ├── auth.py        # Авторизация
│   │   └── shows.py       # Работа с фильмами/сериалами
│   ├── models/            # SQLAlchemy модели
│   │   ├── user.py        # Модель пользователя
│   │   └── show.py        # Модели для контента
│   ├── schemas/           # Pydantic схемы
│   ├── services/          # Бизнес-логика
│   │   └── auth.py        # Сервис авторизации
│   ├── static/            # Статические файлы
│   │   ├── css/
│   │   └── js/
│   ├── templates/         # HTML шаблоны
│   ├── config.py          # Конфигурация
│   ├── database.py        # Подключение к БД
│   ├── load_data.py       # Загрузка данных из CSV
│   └── main.py            # Точка входа FastAPI
├── data/
│   └── netflix.csv        # Данные для загрузки
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── README.md
```

## 🗄 База данных

### Таблицы

- **users** - Пользователи системы
- **shows** - Фильмы и сериалы
- **categories** - Категории (жанры)
- **ratings** - Возрастные рейтинги
- **show_categories** - Связь фильмов с категориями (many-to-many)

### Схема таблицы shows

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Primary Key |
| show_id | String | Внешний ID (из CSV) |
| type | String | Тип: Movie или TV Show |
| title | String | Название |
| director | String | Режиссёр |
| cast | Text | Актёры |
| country | String | Страна |
| date_added | String | Дата добавления |
| release_year | Integer | Год выпуска |
| rating_id | Integer | FK на ratings |
| duration | String | Длительность |
| listed_in | Text | Категории (оригинал) |
| description | Text | Описание |

## 🔌 API Endpoints

### Авторизация

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход (получение токена)
- `GET /api/auth/me` - Информация о текущем пользователе

### Каталог

- `GET /api/shows/` - Список фильмов/сериалов с фильтрами
- `GET /api/shows/{id}` - Информация о конкретном фильме
- `GET /api/shows/categories` - Список категорий
- `GET /api/shows/ratings` - Список рейтингов
- `GET /api/shows/types` - Список типов контента
- `GET /api/shows/countries` - Список стран
- `GET /api/shows/years` - Диапазон годов

## 🐳 Docker Commands

```bash
# Запуск
docker compose up --build

# Запуск в фоне
docker compose up -d --build

# Остановка
docker compose down

# Остановка с удалением данных
docker compose down -v

# Просмотр логов
docker compose logs -f

# Перезапуск приложения
docker compose restart app
```

## ⚙️ Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| DATABASE_URL | URL подключения к PostgreSQL | postgresql://... |
| SECRET_KEY | Секретный ключ для JWT | ... |
| CSV_PATH | Путь к CSV файлу | /app/data/netflix.csv |

## 📊 Данные

Приложение загружает данные из CSV файла Netflix при первом запуске. Данные автоматически:

1. Парсятся с помощью Pandas
2. Разбиваются по категориям и рейтингам
3. Загружаются в PostgreSQL

## 📝 License

MIT

